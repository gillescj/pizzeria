import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
    background: hsl(190, 60%, 60%);
`;

const Map = () => {
    return <Container>Map</Container>;
};

export default Map;
